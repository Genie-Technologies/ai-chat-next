import AppBar from "@mui/material/AppBar";

export const Footer = () => {
  return (
    <AppBar
      position="fixed"
      color="primary"
      sx={{ top: "auto", bottom: 0 }}
    ></AppBar>
  );
};

export default Footer;
